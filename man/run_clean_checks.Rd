% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-clean-checks.R
\name{run_clean_checks}
\alias{run_clean_checks}
\title{Run basic data cleaning checks}
\usage{
run_clean_checks(
  data,
  dataset_name,
  diagnostics_dir = "diagnostics",
  negative_cols = NULL,
  percent_cols = NULL,
  custom_checks = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data}{Data frame to clean}

\item{dataset_name}{Name of dataset (for logging and file naming)}

\item{diagnostics_dir}{Directory to save diagnostic CSV files (created if doesn't exist)}

\item{negative_cols}{Character vector of columns to check for negative values (optional)}

\item{percent_cols}{Character vector of columns to check for percent range (optional)}

\item{custom_checks}{List of custom cleaning functions (optional)}

\item{verbose}{Logical, print detailed progress messages (default: FALSE)}
}
\value{
List with cleaned data, issue log, and all diagnostic details
}
\description{
Main dispatch function to run multiple cleaning operations on a dataset
currently including a function to check for negative values and percent ranges.
}
\examples{
# Basic usage
result <- run_clean_checks(
  data = my_data,
  dataset_name = "water_quality_2024",
  negative_cols = c("temperature", "do_mgl"),
  percent_cols = c("do_pct_sat", "turbidity_pct")
)

# Access cleaned data
clean_data <- result$data

# View issue log
print(result$issue_log)

# Save issue log
write.csv(result$issue_log, "cleaning_log.csv", row.names = FALSE)
}
